<div class="genre_search">
  <table class="table">
    <tr>
      <th>ジャンル検索</th>
      <% if @genres.nil?%>
        <tr>
          <td>ジャンルはまだありません。</td>
        </tr>
      <% else %>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
        <% end %>
      <% end %>

    </tr>
  </table>
</div>